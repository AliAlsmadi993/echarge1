@{
    ViewData["Title"] = "Home";
}

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/home.css" asp-append-version="true" />
}

<style>
    .img-fluid {
        max-width: 100%;
        height: 442px;
        width: 441px;
    }
</style>
<section id="hero">
    <div class="video-background">
        <video autoplay loop muted>
            <source src="~/lib/HOME.mp4" type="video/mp4">
            Your browser does not support the video tag.
        </video>
    </div>

    <div class="hero-content">
        <h1 class="animated-title">Welcome to E-CHARGE</h1>
        <p class="animated-text">The Future of Electric Vehicle Charging</p>
        <a class="btn btn-custom" style="background-color:#1ABC9C" href="#services">Explore Services</a>
    </div>
</section>
<section id="about-us" class="container py-5">
    <div class="row align-items-center">
        <!-- الصورة -->
        <div class="col-lg-6 mb-4 mb-lg-0">
            <img src="~/images/E-CHARGE-HOME-3.jpeg" alt="About E-CHARGE" class="img-fluid rounded-4 shadow-lg w-100">
        </div>

        <!-- النصوص -->
        <div class="col-lg-6">
            <div class="about-text">
                <p class="section-subtitle text-uppercase mb-2">ABOUT US</p>
                <h2 class="fw-bold mb-3">Powering the Future of Electric Vehicles</h2>
                <p class="mb-3">
                    At E-CHARGE, we are committed to transforming the way you charge and interact with electric vehicles. We aim to provide fast, efficient, and accessible charging solutions for every electric vehicle owner.
                </p>
                <div class="bg-light p-4 rounded mb-4">
                    <h4 class="mb-2">Smart Charging Solutions</h4>
                    <p class="mb-0">Our network of charging stations ensures that your electric vehicle is ready for the road, wherever you are.</p>
                </div>
                <a class="btn btn-custom" style="background-color:#1ABC9C" href="@Url.Action("About", "Home")">Learn More</a>
            </div>
        </div>
    </div>
</section>
<section id="ev-services-section" class="container my-5">
    <h2 class="text-center mb-5 fw-bold">Our Services</h2>
    <div class="row g-4 justify-content-center">
        @foreach (var card in ViewBag.ServiceCards)
        {
            <div class="col-sm-12 col-md-6 col-lg-4 d-flex justify-content-center">
                <div class="ev-card animate-ev">
                    <img src="@card.ImageUrl" alt="@card.Title" class="ev-card-img">
                    <div class="ev-card-body">
                        <h3>@card.Title</h3>
                        <p>@card.Description</p>
                        <a href="@card.ButtonLink" class="btn ev-btn">@card.ButtonText</a>
                    </div>
                </div>
            </div>
        }
    </div>
@* </section> *@
@* <section id="why-ev-section" class="my-5"> *@
@*     <div class="container"> *@
@*         <div class="row align-items-center gy-4"> *@
@*             <div class="col-lg-6 position-relative"> *@
@*                 <img src="~/images/E-CHARGE-HOME-2.jpeg" class="img-fluid rounded-4 shadow" alt="EV Users"> *@
@*             </div> *@
@*             <div class="col-lg-6"> *@
@*                 <div class="ev-why-content"> *@
@*                     <h2 class="ev-title">Why Choose E-CHARGE?</h2> *@
@*                     <ul class="ev-feature-list"> *@
@*                         <li> *@
@*                             <span class="ev-icon bg-primary"><i class="fas fa-dollar-sign"></i></span> *@
@*                             Affordable and Transparent Pricing *@
@*                         </li> *@
@*                         <li> *@
@*                             <span class="ev-icon bg-purple"><i class="fas fa-globe"></i></span> *@
@*                             Reliable Nationwide Coverage *@
@*                         </li> *@
@*                         <li> *@
@*                             <span class="ev-icon bg-danger"><i class="fas fa-leaf"></i></span> *@
@*                             Eco-Friendly and Innovative Solutions *@
@*                         </li> *@
@*                     </ul> *@
@*                 </div> *@
@*             </div> *@
@*         </div> *@
@*     </div> *@
@* </section> *@
<section id="ev-testimonials-pro" class="py-5">
    <div class="container">
        <div class="row align-items-center">
            <!-- صورة العميل الثابتة -->
                <div class="col-lg-5 text-center mb-4 mb-lg-0 d-flex justify-content-center align-items-center">
                    <video autoplay muted loop playsinline class="rounded-4 shadow-lg" style="width:100%; height:100%; object-fit:cover; border-radius: 1rem;">
                        <source src="~/lib/TESTOMONUALE3.mp4" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                </div>


            <!-- محتوى الريڤيو المتغير -->
            <div class="col-lg-7 position-relative">
                <div class="testimonial-wrapper">
                    @for (int i = 0; i < ViewBag.Testimonials.Count; i++)
                    {
                        var t = ViewBag.Testimonials[i];
                        <div class="testimonial-slide @(i == 0 ? "active" : "")">
                            <h3 class="client-name">@t.UserName</h3>
                            <h5 class="testimonial-title">@t.Subject</h5>
                            <p class="testimonial-message">@t.Message</p>
                        </div>
                    }
                </div>

                <!-- النقاط السفلية -->
                <div class="testimonial-dots mt-3 text-center">
                    @for (int i = 0; i < ViewBag.Testimonials.Count; i++)
                    {
                        <span class="dot @(i == 0 ? "active-dot" : "")" data-index="@i"></span>
                    }
                </div>
            </div>
        </div>
    </div>
</section>

<section id="ev-stats-minimal" class="py-5">
    <div class="container">
        <h2 class="text-center mb-5 fw-bold">Website Achievements</h2>
        <div class="row justify-content-center text-center gy-4">
            <div class="col-6 col-md-3">
                <div class="ev-stat-box animate-stat">
                    <h3>@ViewBag.Statistics.UserCount+</h3>
                    <p>Registered Users</p>
                </div>
            </div>
            <div class="col-6 col-md-3">
                <div class="ev-stat-box animate-stat">
                    <h3>@ViewBag.Statistics.StationCount+</h3>
                    <p>Charging Stations</p>
                </div>
            </div>
            <div class="col-6 col-md-3">
                <div class="ev-stat-box animate-stat">
                    <h3>@ViewBag.Statistics.ReviewCount+</h3>
                    <p>Total Reviews</p>
                </div>
            </div>
            <div class="col-6 col-md-3">
                <div class="ev-stat-box animate-stat">
                    <h3>@ViewBag.Statistics.TestimonialCount+</h3>
                    <p>Client Testimonials</p>
                </div>
            </div>
        </div>
    </div>
</section>
<script>
    document.addEventListener("DOMContentLoaded", function () {
        const slides = document.querySelectorAll(".testimonial-slide");
        const dots = document.querySelectorAll(".dot");
        let current = 0;

        function showSlide(index) {
            slides.forEach((slide, i) => {
                slide.classList.remove("active");
                dots[i].classList.remove("active-dot");
            });
            slides[index].classList.add("active");
            dots[index].classList.add("active-dot");
        }

        dots.forEach(dot => {
            dot.addEventListener("click", () => {
                current = parseInt(dot.getAttribute("data-index"));
                showSlide(current);
            });
        });

        setInterval(() => {
            current = (current + 1) % slides.length;
            showSlide(current);
        }, 3000);
    });
</script>
